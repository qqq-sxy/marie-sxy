# Copy this file to ".env" in the same directory (or use MARIE_SXY_ENV_FILE).
# ".env" is gitignored — never commit real keys.

# OpenAI (default model in code is gpt-4o-mini; LiteLLM reads this for OpenAI routes)
OPENAI_API_KEY=

# Optional overrides
# MARIE_SXY_MODEL=gpt-4o-mini
# MARIE_SXY_OFFLINE=1
